@model IEnumerable<CleaningSupplies.Database.Models.Master>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsDeleted)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Notes)
        </th>



        <th>
            @Html.DisplayNameFor(model => model.ModifiedByDatetime)
        </th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IsDeleted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.HiddenFor(modelItem => item.CreatedByDateTime)
            </td>
            <td>
                @Html.HiddenFor(modelItem => item.ModifiedByDatetime)
            </td>
            <td>
                @Html.HiddenFor(modelItem => item.CreatedById)
            </td>
            <td>
                @Html.HiddenFor(modelItem => item.ModifiedById)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID }) 
                @Html.ActionLink("Details", "Details", new { id = item.ID })
                @Html.ActionLink("Notes", "Notes", new { id = item.ID})
                @*@Html.ActionLink("Delete", "Delete", new { id = item.ID })*@
            </td>
        </tr>
    }

</table>

